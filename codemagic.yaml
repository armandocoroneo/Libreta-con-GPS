workflows:
  libreta-gps:
    name: Libreta GPS Android

    environment:
      flutter: "3.29.3"

    scripts:
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
