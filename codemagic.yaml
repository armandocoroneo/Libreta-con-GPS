workflows:
  libreta-gps:
    name: Libreta GPS Android

    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter analyze
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
